<%- include("../layouts/navbar") -%>
    <div class="order-page">
        <% if(many==true){%>
            <% for (let i=0; i < algorithm.length; i++) {%>
                <div class="bill-bg">
                    <div class="first-info">
                        <p>Name : <%= data.name%>
                        </p>
                        <p>Phone number : <%= data.phone%>
                        </p>
                    </div>

                    <div class="seconst-info">
                        <p>Date : <%= data.Date%>
                        </p>
                        <p>Bill ID : <%= data.bill_id%>
                        </p>
                    </div>

                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Product</th>
                                <th>Price</th>
                                <th>Qty</th>
                                <th>Total price</th>
                            </tr>
                        </thead>

                        <tbody>

                            <% if(i==0){%>
                                <% for(let j=0; j < algorithm[i][1]; j++){%>
                                    <tr>
                                        <td>
                                            <%= j+1%>
                                        </td>
                                        <td>
                                            <%= data.order[j].name%>
                                        </td>
                                        <td>$<%= data.order[j].price%>
                                        </td>
                                        <td>
                                            <%= data.order[j].qty%>
                                        </td>
                                        <td>$<%= data.order[j].qty * data.order[j].price %>
                                        </td>
                                    </tr>
                                    <%} %>
                                        <%} else {%>
                                            <% for(let j=algorithm[i-1][1]; j < algorithm[i][1]; j++){%>
                                                <tr>
                                                    <td>
                                                        <%= j+1%>
                                                    </td>
                                                    <td>
                                                        <%= data.order[j].name%>
                                                    </td>
                                                    <td>$<%= data.order[j].price%>
                                                    </td>
                                                    <td>
                                                        <%= data.order[j].qty%>
                                                    </td>
                                                    <td>$<%= data.order[j].qty * data.order[j].price %>
                                                    </td>
                                                </tr>
                                                <%} %>
                                                    <%} %>
                        </tbody>
                    </table>
                    <br>
                    <% } %>
                        <%} else {%>
                            <div class="bill-bg">
                                <div class="first-info">
                                    <p>Product : <%= data.name%>
                                    </p>
                                    <p>Phone number : <%= data.phone%>
                                    </p>
                                </div>

                                <div class="seconst-info">
                                    <p>Date : <%= data.Date%>
                                    </p>
                                    <p>Bill ID : <%= data.bill_id%>
                                    </p>
                                </div>

                                <table class="table table-striped">
                                    <thead>
                                        <tr>
                                            <th>#</th>
                                            <th>Product</th>
                                            <th>Price</th>
                                            <th>Qty</th>
                                            <th>Total price</th>
                                        </tr>
                                    </thead>

                                    <tbody>

                                        <% for(let j=0; j < data.order.length; j++){%>
                                            <tr>
                                                <td>
                                                    <%= j+1%>
                                                </td>
                                                <td>
                                                    <%= data.order[j].name%>
                                                </td>
                                                <td>$<%= data.order[j].price%>
                                                </td>
                                                <td>
                                                    <%= data.order[j].qty%>
                                                </td>
                                                <td>$<%= data.order[j].qty * data.order[j].price %>
                                                </td>
                                            </tr>
                                            <%}%>

                                            
                                    </tbody>
                                </table>

                                <%}%>
                            </div>

                            <br>

                            <%- include("../layouts/footer") -%>